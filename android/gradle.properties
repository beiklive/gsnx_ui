# SDL2 源码目录（里面有 android-project/app/src/main/java 的 SDL Java 代码）。
# 用 CMake 的 fetch 模式时，SDL2 会被拉到 build/android/_deps/sdl2-src，把这个路径填进来即可；
# 也可以 git clone https://github.com/libsdl-org/SDL 到任意位置后填这里。
sdl2SourceDir=

android.useAndroidX=false
org.gradle.jvmargs=-Xmx2048m
